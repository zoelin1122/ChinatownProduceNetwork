{
  "name": "3d-lab-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "esbuild fruit.jsx --bundle --minify --jsx=automatic --outfile=fruit.js --servedir=.",
    "build": "esbuild fruit.jsx --bundle --minify --jsx=automatic --outfile=fruit.js"
  },
  "dependencies": {
    "@react-three/drei": "^9.56.24",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-three-fiber": "^6.0.13",
    "react-use-gesture": "^9.1.3",
    "three": "^0.149.0"
  },
  "devDependencies": {
    "esbuild": "^0.17.18"
  }
}
